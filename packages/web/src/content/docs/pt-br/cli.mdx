---
title: CLI
description: Opções e comandos da CLI do selene.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do selene, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
selene
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o selene programaticamente.

```bash
selene run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do selene.

```bash
selene [project]
```

#### Opções

| Flag         | Curto | Descrição                                                                  |
| ------------ | ----- | -------------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue a última sessão                                                   |
| `--session`  | `-s`  | ID da sessão para continuar                                                |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| `--prompt`   |       | Prompt a ser usado                                                         |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| `--agent`    |       | Agente a ser usado                                                         |
| `--port`     |       | Porta para escutar                                                         |
| `--hostname` |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do selene também possui os seguintes comandos.

---

### agent

Gerencie agentes para o selene.

```bash
selene agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do selene já em execução, iniciado via comandos `serve` ou `web`.

```bash
selene attach [url]
```

Isso permite usar o TUI com um backend selene remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
selene web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
selene attach http://10.20.30.40:4096
```

#### Opções

| Flag        | Curto | Descrição                                |
| ----------- | ----- | ---------------------------------------- |
| `--dir`     |       | Diretório de trabalho para iniciar o TUI |
| `--session` | `-s`  | ID da sessão para continuar              |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
selene agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
selene agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
selene auth [command]
```

---

#### login

O selene é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `selene auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/selene/auth.json`.

```bash
selene auth login
```

Quando o selene é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
selene auth list
```

Ou a versão curta.

```bash
selene auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
selene auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
selene github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
selene github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
selene github run
```

##### Opções

| Flag      | Descrição                                        |
| --------- | ------------------------------------------------ |
| `--event` | Evento simulado do GitHub para executar o agente |
| `--token` | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
selene mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
selene mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
selene mcp list
```

Ou use a versão curta.

```bash
selene mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
selene mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
selene mcp auth list
```

Ou use a versão curta.

```bash
selene mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
selene mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
selene mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
selene models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
selene models anthropic
```

#### Opções

| Flag        | Descrição                                                             |
| ----------- | --------------------------------------------------------------------- |
| `--refresh` | Atualiza o cache de modelos a partir do models.dev                    |
| `--verbose` | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no selene.

```bash
selene models --refresh
```

---

### run

Execute o selene em modo não interativo passando um prompt diretamente.

```bash
selene run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "selene run"
selene run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `selene serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
selene serve

# Em outro terminal, execute comandos que se anexam a ele
selene run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag         | Curto | Descrição                                                                     |
| ------------ | ----- | ----------------------------------------------------------------------------- |
| `--command`  |       | O comando a ser executado, use mensagem para argumentos                       |
| `--continue` | `-c`  | Continue a última sessão                                                      |
| `--session`  | `-s`  | ID da sessão para continuar                                                   |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)    |
| `--share`    |       | Compartilhe a sessão                                                          |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                                 |
| `--agent`    |       | Agente a ser usado                                                            |
| `--file`     | `-f`  | Arquivo(s) a serem anexados à mensagem                                        |
| `--format`   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                     |
| `--title`    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)    |
| `--attach`   |       | Anexe a um servidor selene em execução (por exemplo, http://localhost:4096) |
| `--port`     |       | Porta para o servidor local (padrão para porta aleatória)                     |

---

### serve

Inicie um servidor selene sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
selene serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do selene sem a interface TUI. Defina `SELENE_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `selene`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do selene.

```bash
selene session [command]
```

---

#### list

Liste todas as sessões do selene.

```bash
selene session list
```

##### Opções

| Flag          | Curto | Descrição                                 |
| ------------- | ----- | ----------------------------------------- |
| `--max-count` | `-n`  | Limitar às N sessões mais recentes        |
| `--format`    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do selene.

```bash
selene stats
```

#### Opções

| Flag        | Descrição                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------- |
| `--days`    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| `--tools`   | Número de ferramentas a serem mostradas (todas)                                                      |
| `--models`  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| `--project` | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
selene export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do selene.

```bash
selene import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do selene.

```bash
selene import session.json
selene import https://opncd.ai/s/abc123
```

---

### web

Inicie um servidor selene sem cabeça com uma interface web.

```bash
selene web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o selene através de uma interface web. Defina `SELENE_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `selene`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
selene acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag         | Descrição                 |
| ------------ | ------------------------- |
| `--cwd`      | Diretório de trabalho     |
| `--port`     | Porta para escutar        |
| `--hostname` | Nome do host para escutar |

---

### uninstall

Desinstale o selene e remova todos os arquivos relacionados.

```bash
selene uninstall
```

#### Opções

| Flag            | Curto | Descrição                                |
| --------------- | ----- | ---------------------------------------- |
| `--keep-config` | `-c`  | Manter arquivos de configuração          |
| `--keep-data`   | `-d`  | Manter dados de sessão e snapshots       |
| `--dry-run`     |       | Mostrar o que seria removido sem remover |
| `--force`       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o selene para a versão mais recente ou uma versão específica.

```bash
selene upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
selene upgrade
```

Para atualizar para uma versão específica.

```bash
selene upgrade v0.1.48
```

#### Opções

| Flag       | Curto | Descrição                                                        |
| ---------- | ----- | ---------------------------------------------------------------- |
| `--method` | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do selene aceita as seguintes flags globais.

| Flag           | Curto | Descrição                               |
| -------------- | ----- | --------------------------------------- |
| `--help`       | `-h`  | Exibir ajuda                            |
| `--version`    | `-v`  | Imprimir número da versão               |
| `--print-logs` |       | Imprimir logs no stderr                 |
| `--log-level`  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O selene pode ser configurado usando variáveis de ambiente.

| Variável                              | Tipo    | Descrição                                                             |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `SELENE_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                  |
| `SELENE_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                      |
| `SELENE_CONFIG`                     | string  | Caminho para o arquivo de configuração                                |
| `SELENE_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                              |
| `SELENE_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                  |
| `SELENE_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                    |
| `SELENE_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                   |
| `SELENE_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal            |
| `SELENE_PERMISSION`                 | string  | Configuração de permissões json inline                                |
| `SELENE_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                            |
| `SELENE_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                     |
| `SELENE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                       |
| `SELENE_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                        |
| `SELENE_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)               |
| `SELENE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                          |
| `SELENE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                          |
| `SELENE_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                        |
| `SELENE_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                 |
| `SELENE_DISABLE_FILETIME_CHECK`     | boolean | Desabilitar verificação de tempo de arquivo para otimização           |
| `SELENE_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                             |
| `SELENE_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                                |
| `SELENE_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                      |
| `SELENE_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `selene`) |
| `SELENE_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                 |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                        | Tipo    | Descrição                                                 |
| ----------------------------------------------- | ------- | --------------------------------------------------------- |
| `SELENE_EXPERIMENTAL`                         | boolean | Habilitar todos os recursos experimentais                 |
| `SELENE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `SELENE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `SELENE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `SELENE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `SELENE_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `SELENE_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `SELENE_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `SELENE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `SELENE_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `SELENE_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar verificação de tipo LSP experimental            |
| `SELENE_EXPERIMENTAL_MARKDOWN`                | boolean | Habilitar recursos experimentais de markdown              |
| `SELENE_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
